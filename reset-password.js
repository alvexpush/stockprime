(function(){
  const form=document.querySelector("[data-reset-form]"),message=document.querySelector("[data-message]"),token=new URLSearchParams(location.search).get("token")||"";
  if(!token){message.textContent="This reset link is incomplete. Request a new one.";message.style.color="#B3132C";form.querySelector("button").disabled=true;return}
  form.onsubmit=async event=>{event.preventDefault();const button=form.querySelector("button"),loginCode=form.loginCode.value;if(!/^\d{6}$/.test(loginCode)){message.textContent="Choose a six-digit login code.";message.style.color="#B3132C";return}button.disabled=true;button.textContent="Updating...";try{const response=await fetch("/api/password/reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,loginCode,loginCodeConfirmation:form.confirmation.value})}),data=await response.json();if(!response.ok)throw new Error(data.error);message.textContent=data.message;message.style.color="#08752e";setTimeout(()=>location.replace("login.html"),1200)}catch(error){message.textContent=error.message;message.style.color="#B3132C";button.disabled=false;button.textContent="Reset Login Code"}};
})();
