{
  "name": "stockprime",
  "version": "1.0.0",
  "private": true,
  "description": "StockPrime customer and operations platform",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:check": "node scripts/check-db.js",
    "production:prepare": "node scripts/prepare-production-env.js",
    "email:authorize": "node scripts/authorize-zoho-email.js",
    "email:check": "node scripts/check-zoho-email.js",
    "test:email": "node scripts/check-transactional-email-flows.js",
    "test:e2e": "node scripts/e2e-flow.js"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1"
  }
}
