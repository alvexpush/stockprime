(function(){
const form=document.querySelector('form[action="dashboard.html"]');if(!form)return;
const message=document.createElement("div");message.style.cssText="display:none;margin-bottom:12px;padding:10px;border:1px solid #fcd04a66;border-radius:6px;background:#fcd04a12;color:#fcd04a;font-size:12px";form.prepend(message);
form.addEventListener("submit",async event=>{event.preventDefault();message.style.display="none";const email=form.email.value.trim(),loginCode=form.login_code.value;if(!email||!loginCode)return;const button=form.querySelector('[type="submit"]');button.disabled=true;button.textContent="Checking account…";try{const response=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"same-origin",body:JSON.stringify({email,loginCode})}),data=await response.json();if(!response.ok){message.textContent=data.error||"Sign in failed.";message.style.display="block";return}localStorage.setItem("stockprimeSession",JSON.stringify(data.user));const requested=new URLSearchParams(location.search).get("next");location.assign(requested&&/^[a-z0-9._?=&-]+$/i.test(requested)?requested:"dashboard.html")}catch{message.textContent="Cannot reach the application server. Start it and try again.";message.style.display="block"}finally{button.disabled=false;button.textContent="Sign In"}});
})();
